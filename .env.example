# Database Configuration
DATABASE_URL="postgresql://usuario:password@host:5432/database?schema=schema_name"

# JWT Configuration
JWT_SECRET="tu_clave_secreta_super_segura_aqui"
JWT_EXPIRES_IN="8h"

# Node Environment
NODE_ENV="production"

# Server Configuration (opcional)
PORT=3000
HOST="localhost"

# CORS Configuration
# Lista de orígenes permitidos separados por comas
# Para desarrollo: localhost
# Para producción: tu dominio de frontend
# Para permitir cualquier origen (NO RECOMENDADO en producción): usa "*"
ALLOWED_ORIGINS="http://localhost:3000,http://localhost:3001,https://tu-frontend.vercel.app"
